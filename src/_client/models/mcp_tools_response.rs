/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.6.4
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// McpToolsResponse : The authorized catalog for one stored server.  Carries no server URL, no credential, and no allowlist entry that the live catalog did not return (R-DISC-2). ``server_revision`` is what an application persists with a proposed call and sends back to ``/api/v1/mcp/execute``, so a stored-configuration change between the two is refused rather than executed.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct McpToolsResponse {
    #[serde(rename = "server_id")]
    pub server_id: uuid::Uuid,
    /// An opaque revision of the stored server's URL, credential, enabled state and allowlist. It detects Otari-side and platform-side configuration changes only: a remote server that changes its own catalog or a tool's behavior behind an unchanged URL will not move it.
    #[serde(rename = "server_revision")]
    pub server_revision: String,
    #[serde(rename = "tools")]
    pub tools: Vec<models::McpToolDefinition>,
    #[serde(rename = "warnings")]
    pub warnings: Vec<models::McpToolWarning>,
}

impl McpToolsResponse {
    /// The authorized catalog for one stored server.  Carries no server URL, no credential, and no allowlist entry that the live catalog did not return (R-DISC-2). ``server_revision`` is what an application persists with a proposed call and sends back to ``/api/v1/mcp/execute``, so a stored-configuration change between the two is refused rather than executed.
    pub fn new(
        server_id: uuid::Uuid,
        server_revision: String,
        tools: Vec<models::McpToolDefinition>,
        warnings: Vec<models::McpToolWarning>,
    ) -> McpToolsResponse {
        McpToolsResponse {
            server_id,
            server_revision,
            tools,
            warnings,
        }
    }
}
