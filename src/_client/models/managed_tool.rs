/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ManagedTool : One tool the gateway can run itself.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ManagedTool {
    /// Every `tools[].type` this deployment currently routes to the tool. Always includes the canonical `otari_*` type; for web search it also includes the provider-named keywords when interception is enabled.
    #[serde(rename = "accepted_types")]
    pub accepted_types: Vec<String>,
    /// Whether this deployment has a backend configured for the tool. A request declaring an unavailable tool is rejected with 400.
    #[serde(rename = "available")]
    pub available: bool,
    /// What the tool does, as the model is told.
    #[serde(rename = "description")]
    pub description: String,
    /// A ready-to-use `tools[]` entry.
    #[serde(rename = "example")]
    pub example: std::collections::HashMap<String, serde_json::Value>,
    /// The canonical tool type to put in `tools[]`.
    #[serde(rename = "id")]
    pub id: String,
    /// JSON Schema for the arguments the model supplies, as the model sees it.
    #[serde(rename = "input_schema")]
    pub input_schema: std::collections::HashMap<String, serde_json::Value>,
    #[serde(rename = "object", skip_serializing_if = "Option::is_none")]
    pub object: Option<Object>,
}

impl ManagedTool {
    /// One tool the gateway can run itself.
    pub fn new(
        accepted_types: Vec<String>,
        available: bool,
        description: String,
        example: std::collections::HashMap<String, serde_json::Value>,
        id: String,
        input_schema: std::collections::HashMap<String, serde_json::Value>,
    ) -> ManagedTool {
        ManagedTool {
            accepted_types,
            available,
            description,
            example,
            id,
            input_schema,
            object: None,
        }
    }
}
///
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Object {
    #[serde(rename = "tool")]
    Tool,
}

impl Default for Object {
    fn default() -> Object {
        Self::Tool
    }
}
