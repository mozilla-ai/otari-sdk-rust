/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.12.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// CatalogResponse : The grouped catalog, and the facts a reader needs to interpret its prices.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CatalogResponse {
    /// Models matching the search, before the window, so a caller can page without reading them all.
    #[serde(rename = "count")]
    pub count: i32,
    /// Whether an unpriced model is metered at the genai-prices default.
    #[serde(rename = "default_pricing")]
    pub default_pricing: bool,
    /// When the accepted genai-prices snapshot was taken. Null while the bundled dataset serves.
    #[serde(rename = "defaults_as_of", deserialize_with = "Option::deserialize")]
    pub defaults_as_of: Option<chrono::DateTime<chrono::FixedOffset>>,
    /// False when models.dev could not be read; descriptions are then absent.
    #[serde(rename = "metadata_available")]
    pub metadata_available: bool,
    #[serde(rename = "models")]
    pub models: Vec<models::CatalogModelSummary>,
}

impl CatalogResponse {
    /// The grouped catalog, and the facts a reader needs to interpret its prices.
    pub fn new(
        count: i32,
        default_pricing: bool,
        defaults_as_of: Option<chrono::DateTime<chrono::FixedOffset>>,
        metadata_available: bool,
        models: Vec<models::CatalogModelSummary>,
    ) -> CatalogResponse {
        CatalogResponse {
            count,
            default_pricing,
            defaults_as_of,
            metadata_available,
            models,
        }
    }
}
