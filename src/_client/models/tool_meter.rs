/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ToolMeter : One gateway-run tool's call counts on a request.  A TypedDict rather than a model, and every key optional, so a stored entry serializes back as it was written: a model with defaults would put ``\"unit_rate\": null`` on every tool that has never been priced.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ToolMeter {
    #[serde(rename = "billed", skip_serializing_if = "Option::is_none")]
    pub billed: Option<i32>,
    #[serde(rename = "errors", skip_serializing_if = "Option::is_none")]
    pub errors: Option<i32>,
    #[serde(
        rename = "unit_rate",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub unit_rate: Option<Option<f64>>,
}

impl ToolMeter {
    /// One gateway-run tool's call counts on a request.  A TypedDict rather than a model, and every key optional, so a stored entry serializes back as it was written: a model with defaults would put ``\"unit_rate\": null`` on every tool that has never been priced.
    pub fn new() -> ToolMeter {
        ToolMeter {
            billed: None,
            errors: None,
            unit_rate: None,
        }
    }
}
