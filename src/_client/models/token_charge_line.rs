/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// TokenChargeLine : A charge line billed per million tokens.  ``rate_per_million`` is the discriminator: its presence (rather than ``unit_rate``) is what tells a reader, and a renderer, which unit convention applies. See ``gateway.services.pricing_service``.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct TokenChargeLine {
    /// USD charged for this line.
    #[serde(rename = "cost")]
    pub cost: f64,
    /// What was metered, e.g. 'input' or 'cache_read'.
    #[serde(rename = "meter")]
    pub meter: String,
    /// USD per million units.
    #[serde(rename = "rate_per_million")]
    pub rate_per_million: f64,
    #[serde(rename = "units")]
    pub units: Box<models::Units>,
}

impl TokenChargeLine {
    /// A charge line billed per million tokens.  ``rate_per_million`` is the discriminator: its presence (rather than ``unit_rate``) is what tells a reader, and a renderer, which unit convention applies. See ``gateway.services.pricing_service``.
    pub fn new(
        cost: f64,
        meter: String,
        rate_per_million: f64,
        units: models::Units,
    ) -> TokenChargeLine {
        TokenChargeLine {
            cost,
            meter,
            rate_per_million,
            units: Box::new(units),
        }
    }
}
