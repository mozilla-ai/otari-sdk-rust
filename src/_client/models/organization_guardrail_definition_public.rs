/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.12.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// OrganizationGuardrailDefinitionPublic : The API-facing shape. Carries the names of the stored secrets and none of their values.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct OrganizationGuardrailDefinitionPublic {
    /// Whether the worker that answered this request holds a guardrail built from this version of the definition. built means it does and the check runs; failed means that worker tried these exact arguments and could not build them, so every mandate pointing here is unevaluable; pending means it holds nothing for this version yet, which is the answer right after a write and on any worker that has not caught up within the refresh interval; disabled means the definition is switched off and nothing is built on purpose. It answers for one worker, so two reads can disagree while a write propagates. Why a build failed is never reported here: the reason is in the gateway's log
    #[serde(rename = "build_state")]
    pub build_state: BuildState,
    /// The constructor arguments the catalog does not mark secret, as they were stored. Returned in clear: the secrets were taken out of this map by flag, and a form has to round-trip an endpoint or a project id
    #[serde(rename = "create_kwargs")]
    pub create_kwargs: std::collections::HashMap<String, serde_json::Value>,
    /// The secret constructor arguments this definition holds, each as ***. Sending one back unchanged keeps the stored value; sending a new one rotates it, and leaving one out clears it
    #[serde(rename = "create_secrets")]
    pub create_secrets: std::collections::HashMap<String, String>,
    #[serde(rename = "created_at")]
    pub created_at: String,
    #[serde(rename = "enabled")]
    pub enabled: bool,
    #[serde(rename = "guardrail_name")]
    pub guardrail_name: String,
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    #[serde(rename = "name")]
    pub name: String,
    #[serde(rename = "organization_id")]
    pub organization_id: uuid::Uuid,
    /// False when the stored secrets cannot be read with the current OTARI_SECRET_KEY, in which case create_secrets is empty and the definition needs its credentials sent again
    #[serde(rename = "secrets_decryptable")]
    pub secrets_decryptable: bool,
    #[serde(rename = "updated_at")]
    pub updated_at: String,
}

impl OrganizationGuardrailDefinitionPublic {
    /// The API-facing shape. Carries the names of the stored secrets and none of their values.
    pub fn new(
        build_state: BuildState,
        create_kwargs: std::collections::HashMap<String, serde_json::Value>,
        create_secrets: std::collections::HashMap<String, String>,
        created_at: String,
        enabled: bool,
        guardrail_name: String,
        id: uuid::Uuid,
        name: String,
        organization_id: uuid::Uuid,
        secrets_decryptable: bool,
        updated_at: String,
    ) -> OrganizationGuardrailDefinitionPublic {
        OrganizationGuardrailDefinitionPublic {
            build_state,
            create_kwargs,
            create_secrets,
            created_at,
            enabled,
            guardrail_name,
            id,
            name,
            organization_id,
            secrets_decryptable,
            updated_at,
        }
    }
}
/// Whether the worker that answered this request holds a guardrail built from this version of the definition. built means it does and the check runs; failed means that worker tried these exact arguments and could not build them, so every mandate pointing here is unevaluable; pending means it holds nothing for this version yet, which is the answer right after a write and on any worker that has not caught up within the refresh interval; disabled means the definition is switched off and nothing is built on purpose. It answers for one worker, so two reads can disagree while a write propagates. Why a build failed is never reported here: the reason is in the gateway's log
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum BuildState {
    #[serde(rename = "built")]
    Built,
    #[serde(rename = "failed")]
    Failed,
    #[serde(rename = "pending")]
    Pending,
    #[serde(rename = "disabled")]
    Disabled,
}

impl Default for BuildState {
    fn default() -> BuildState {
        Self::Built
    }
}
