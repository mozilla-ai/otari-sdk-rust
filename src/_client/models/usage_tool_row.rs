/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// UsageToolRow : Spend and volume for one gateway-run tool inside the window.  ``calls`` counts billable calls, not requests: one request can run a tool several times, which is the whole reason a per-tool view exists. ``errors`` counts calls that failed and were therefore never billed. ``requests`` is how many requests ran the tool at least once, so the number reconciles with the Activity list when the same filter is applied there.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct UsageToolRow {
    #[serde(rename = "calls")]
    pub calls: i32,
    #[serde(rename = "cost")]
    pub cost: f64,
    #[serde(rename = "errors")]
    pub errors: i32,
    #[serde(rename = "requests")]
    pub requests: i32,
    #[serde(rename = "tool")]
    pub tool: String,
}

impl UsageToolRow {
    /// Spend and volume for one gateway-run tool inside the window.  ``calls`` counts billable calls, not requests: one request can run a tool several times, which is the whole reason a per-tool view exists. ``errors`` counts calls that failed and were therefore never billed. ``requests`` is how many requests ran the tool at least once, so the number reconciles with the Activity list when the same filter is applied there.
    pub fn new(calls: i32, cost: f64, errors: i32, requests: i32, tool: String) -> UsageToolRow {
        UsageToolRow {
            calls,
            cost,
            errors,
            requests,
            tool,
        }
    }
}
