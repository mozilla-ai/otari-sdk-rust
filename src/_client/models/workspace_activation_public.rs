/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// WorkspaceActivationPublic : Where a workspace stands on its first successful request.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct WorkspaceActivationPublic {
    /// The first request that succeeded, which is the guide's receipt. Null until one does.
    #[serde(
        rename = "activation_attempt",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub activation_attempt: Option<Option<Box<models::ActivationAttemptPublic>>>,
    /// Whether someone skipped the guide for this workspace.
    #[serde(rename = "dismissed")]
    pub dismissed: bool,
    /// Whether the dashboard should offer the guide to this caller right now: the deployment has it enabled, the workspace is classified for it, nobody dismissed it, no request has succeeded yet, and the caller may manage the workspace.
    #[serde(rename = "experience_eligible")]
    pub experience_eligible: bool,
    /// The most recent request, so a failure can be reported while the guide keeps waiting.
    #[serde(
        rename = "latest_attempt",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub latest_attempt: Option<Option<Box<models::ActivationAttemptPublic>>>,
    /// 'activated' once a gateway request in this workspace has succeeded, 'failed' when the last one failed and none has yet succeeded, 'waiting' when there has been none at all.
    #[serde(rename = "status")]
    pub status: Status,
}

impl WorkspaceActivationPublic {
    /// Where a workspace stands on its first successful request.
    pub fn new(
        dismissed: bool,
        experience_eligible: bool,
        status: Status,
    ) -> WorkspaceActivationPublic {
        WorkspaceActivationPublic {
            activation_attempt: None,
            dismissed,
            experience_eligible,
            latest_attempt: None,
            status,
        }
    }
}
/// 'activated' once a gateway request in this workspace has succeeded, 'failed' when the last one failed and none has yet succeeded, 'waiting' when there has been none at all.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Status {
    #[serde(rename = "waiting")]
    Waiting,
    #[serde(rename = "failed")]
    Failed,
    #[serde(rename = "activated")]
    Activated,
}

impl Default for Status {
    fn default() -> Status {
        Self::Waiting
    }
}
