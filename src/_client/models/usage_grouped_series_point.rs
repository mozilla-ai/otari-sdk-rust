/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// UsageGroupedSeriesPoint : One (time bucket, group) cell of a grouped series.  ``key``/``is_other`` follow the ``UsageGroupRow`` convention: ``key=None`` with ``is_other=True`` is the fold of groups outside the top N, ``key=None`` with ``is_other=False`` is a real NULL group (e.g. a deleted user). ``tokens`` is the *billed* total (input including cache, plus output), the same quantity the ungrouped series' composition fields sum to.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct UsageGroupedSeriesPoint {
    #[serde(rename = "bucket_start")]
    pub bucket_start: String,
    #[serde(rename = "cost")]
    pub cost: f64,
    #[serde(rename = "is_other", skip_serializing_if = "Option::is_none")]
    pub is_other: Option<bool>,
    #[serde(rename = "key", deserialize_with = "Option::deserialize")]
    pub key: Option<String>,
    #[serde(rename = "requests")]
    pub requests: i32,
    #[serde(rename = "tokens")]
    pub tokens: i32,
}

impl UsageGroupedSeriesPoint {
    /// One (time bucket, group) cell of a grouped series.  ``key``/``is_other`` follow the ``UsageGroupRow`` convention: ``key=None`` with ``is_other=True`` is the fold of groups outside the top N, ``key=None`` with ``is_other=False`` is a real NULL group (e.g. a deleted user). ``tokens`` is the *billed* total (input including cache, plus output), the same quantity the ungrouped series' composition fields sum to.
    pub fn new(
        bucket_start: String,
        cost: f64,
        key: Option<String>,
        requests: i32,
        tokens: i32,
    ) -> UsageGroupedSeriesPoint {
        UsageGroupedSeriesPoint {
            bucket_start,
            cost,
            is_other: None,
            key,
            requests,
            tokens,
        }
    }
}
