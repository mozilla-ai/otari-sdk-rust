/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.12.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// OrganizationGuardrailDefinitionCreate : Request body for defining a guardrail Otari will build and call itself.  ``create_kwargs`` carries both halves of the form: the plain arguments and the vendor credentials. Which is which is the catalog's answer, not this schema's, so a credential lands in the encrypted map whatever it is called and a caller cannot move one into the plain column by naming it oddly.  Per-call arguments are not offered. This row says what a guardrail is built with; what travels with each check is the mandate's ``validate_kwargs``.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct OrganizationGuardrailDefinitionCreate {
    /// Constructor arguments for the guardrail. Every argument the catalog marks secret is encrypted at rest and never returned; the rest are stored and returned as sent
    #[serde(rename = "create_kwargs", skip_serializing_if = "Option::is_none")]
    pub create_kwargs: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// False stops the guardrail everywhere it is mandated
    #[serde(rename = "enabled", skip_serializing_if = "Option::is_none")]
    pub enabled: Option<bool>,
    /// The any-guardrail class to build, as the built-in guardrail catalog names it
    #[serde(rename = "guardrail_name")]
    pub guardrail_name: String,
    /// The organization's own label for this definition, unique within the organization
    #[serde(rename = "name")]
    pub name: String,
}

impl OrganizationGuardrailDefinitionCreate {
    /// Request body for defining a guardrail Otari will build and call itself.  ``create_kwargs`` carries both halves of the form: the plain arguments and the vendor credentials. Which is which is the catalog's answer, not this schema's, so a credential lands in the encrypted map whatever it is called and a caller cannot move one into the plain column by naming it oddly.  Per-call arguments are not offered. This row says what a guardrail is built with; what travels with each check is the mandate's ``validate_kwargs``.
    pub fn new(guardrail_name: String, name: String) -> OrganizationGuardrailDefinitionCreate {
        OrganizationGuardrailDefinitionCreate {
            create_kwargs: None,
            enabled: None,
            guardrail_name,
            name,
        }
    }
}
