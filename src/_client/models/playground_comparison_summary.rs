/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PlaygroundComparisonSummary : A row in the comparison history: the question, the pair, the verdict.  Deliberately without the two answers. The list shows a dozen rows at once and none of them renders an answer body, so sending them would move megabytes to draw a few lines of text. There is no detail endpoint either, because the page has no screen that reads one back: a comparison is a judgment that was recorded, not a transcript to resume.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PlaygroundComparisonSummary {
    #[serde(rename = "created_at")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    #[serde(rename = "model_a")]
    pub model_a: String,
    #[serde(rename = "model_b")]
    pub model_b: String,
    #[serde(rename = "preference")]
    pub preference: String,
    #[serde(rename = "user_question")]
    pub user_question: String,
    #[serde(rename = "workspace_id")]
    pub workspace_id: uuid::Uuid,
}

impl PlaygroundComparisonSummary {
    /// A row in the comparison history: the question, the pair, the verdict.  Deliberately without the two answers. The list shows a dozen rows at once and none of them renders an answer body, so sending them would move megabytes to draw a few lines of text. There is no detail endpoint either, because the page has no screen that reads one back: a comparison is a judgment that was recorded, not a transcript to resume.
    pub fn new(
        created_at: chrono::DateTime<chrono::FixedOffset>,
        id: uuid::Uuid,
        model_a: String,
        model_b: String,
        preference: String,
        user_question: String,
        workspace_id: uuid::Uuid,
    ) -> PlaygroundComparisonSummary {
        PlaygroundComparisonSummary {
            created_at,
            id,
            model_a,
            model_b,
            preference,
            user_question,
            workspace_id,
        }
    }
}
