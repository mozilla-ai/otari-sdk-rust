/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PasswordResponse : What the identity signs in with now.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PasswordResponse {
    /// The address this identity signs in with.
    #[serde(rename = "email")]
    pub email: String,
    /// Whether POST /v1/auth/session has stopped accepting the master key as a dashboard login. True once the operator identity has a password, which is what claiming the deployment means; a member setting their own password leaves an unclaimed deployment on the master key. Either way the master key stays the credential for the management API.
    #[serde(rename = "master_key_sign_in_retired")]
    pub master_key_sign_in_retired: bool,
}

impl PasswordResponse {
    /// What the identity signs in with now.
    pub fn new(email: String, master_key_sign_in_retired: bool) -> PasswordResponse {
        PasswordResponse {
            email,
            master_key_sign_in_retired,
        }
    }
}
