/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.6.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// McpToolDefinition : One live tool a caller-orchestrated application may expose to its model.  ``annotations`` is the remote server's own metadata, passed through as untrusted data. Otari never turns ``readOnlyHint`` into an authorization decision (R-RISK-1); each application owns its risk policy, and a server cannot waive an application's approval gate by labeling itself read-only.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct McpToolDefinition {
    /// Provider-native request fields used as defaults (e.g. exa's 'type', searxng's 'engines').
    #[serde(
        rename = "annotations",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub annotations: Option<Option<std::collections::HashMap<String, serde_json::Value>>>,
    /// The server's own description, untrusted.
    #[serde(
        rename = "description",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub description: Option<Option<String>>,
    /// The tool's MCP inputSchema, unmodified.
    #[serde(rename = "input_schema")]
    pub input_schema: std::collections::HashMap<String, serde_json::Value>,
    /// The remote MCP tool name to send back to /api/v1/mcp/execute.
    #[serde(rename = "name")]
    pub name: String,
}

impl McpToolDefinition {
    /// One live tool a caller-orchestrated application may expose to its model.  ``annotations`` is the remote server's own metadata, passed through as untrusted data. Otari never turns ``readOnlyHint`` into an authorization decision (R-RISK-1); each application owns its risk policy, and a server cannot waive an application's approval gate by labeling itself read-only.
    pub fn new(
        input_schema: std::collections::HashMap<String, serde_json::Value>,
        name: String,
    ) -> McpToolDefinition {
        McpToolDefinition {
            annotations: None,
            description: None,
            input_schema,
            name,
        }
    }
}
