/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.12.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// OverviewSummaryResponse : What the dashboard overview renders beside its usage chart.  ``budgets`` and ``ceilings`` are null where the caller may not see them, which is not the same as a strip with nothing in it: deployment budgets are the operator's, and spend ceilings are an organization owner's or admin's.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct OverviewSummaryResponse {
    #[serde(rename = "active_keys")]
    pub active_keys: i32,
    /// Active members of the named workspace; 0 when none is named.
    #[serde(rename = "active_members")]
    pub active_members: i32,
    #[serde(rename = "budgets", deserialize_with = "Option::deserialize")]
    pub budgets: Option<Box<models::AllocationHealthResponse>>,
    #[serde(rename = "ceilings", deserialize_with = "Option::deserialize")]
    pub ceilings: Option<Box<models::AllocationHealthResponse>>,
}

impl OverviewSummaryResponse {
    /// What the dashboard overview renders beside its usage chart.  ``budgets`` and ``ceilings`` are null where the caller may not see them, which is not the same as a strip with nothing in it: deployment budgets are the operator's, and spend ceilings are an organization owner's or admin's.
    pub fn new(
        active_keys: i32,
        active_members: i32,
        budgets: Option<models::AllocationHealthResponse>,
        ceilings: Option<models::AllocationHealthResponse>,
    ) -> OverviewSummaryResponse {
        OverviewSummaryResponse {
            active_keys,
            active_members,
            budgets: if let Some(x) = budgets {
                Some(Box::new(x))
            } else {
                None
            },
            ceilings: if let Some(x) = ceilings {
                Some(Box::new(x))
            } else {
                None
            },
        }
    }
}
