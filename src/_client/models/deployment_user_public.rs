/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// DeploymentUserPublic : An identity on this deployment, whatever organization it belongs to.  Not ``ActiveOrganizationMemberPublic``: that shape is a *membership* joined to an identity, scoped to one organization and hiding the suspended rows. This one is the identity itself, and its ``organizations`` list carries every membership at whatever status, because an account whose only membership is suspended is precisely what an operator comes here to find.  ``is_bootstrap_operator`` and ``is_self`` are the two rows an operator may not deactivate or demote, and they travel on the row so the page can disable those controls rather than offering ones the server refuses. Neither is an authorization: the server refuses either way. ``is_self`` is answered here because nothing else the dashboard fetches names the caller's identity, so without it the page could not tell which row is the reader's own.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct DeploymentUserPublic {
    #[serde(rename = "created_at")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "email", deserialize_with = "Option::deserialize")]
    pub email: Option<String>,
    #[serde(rename = "full_name", deserialize_with = "Option::deserialize")]
    pub full_name: Option<String>,
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    #[serde(rename = "is_active")]
    pub is_active: bool,
    #[serde(rename = "is_bootstrap_operator")]
    pub is_bootstrap_operator: bool,
    #[serde(rename = "is_self")]
    pub is_self: bool,
    #[serde(rename = "is_superuser")]
    pub is_superuser: bool,
    #[serde(rename = "last_sign_in_at", deserialize_with = "Option::deserialize")]
    pub last_sign_in_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    #[serde(rename = "organizations")]
    pub organizations: Vec<models::DeploymentUserOrganizationPublic>,
}

impl DeploymentUserPublic {
    /// An identity on this deployment, whatever organization it belongs to.  Not ``ActiveOrganizationMemberPublic``: that shape is a *membership* joined to an identity, scoped to one organization and hiding the suspended rows. This one is the identity itself, and its ``organizations`` list carries every membership at whatever status, because an account whose only membership is suspended is precisely what an operator comes here to find.  ``is_bootstrap_operator`` and ``is_self`` are the two rows an operator may not deactivate or demote, and they travel on the row so the page can disable those controls rather than offering ones the server refuses. Neither is an authorization: the server refuses either way. ``is_self`` is answered here because nothing else the dashboard fetches names the caller's identity, so without it the page could not tell which row is the reader's own.
    pub fn new(
        created_at: chrono::DateTime<chrono::FixedOffset>,
        email: Option<String>,
        full_name: Option<String>,
        id: uuid::Uuid,
        is_active: bool,
        is_bootstrap_operator: bool,
        is_self: bool,
        is_superuser: bool,
        last_sign_in_at: Option<chrono::DateTime<chrono::FixedOffset>>,
        organizations: Vec<models::DeploymentUserOrganizationPublic>,
    ) -> DeploymentUserPublic {
        DeploymentUserPublic {
            created_at,
            email,
            full_name,
            id,
            is_active,
            is_bootstrap_operator,
            is_self,
            is_superuser,
            last_sign_in_at,
            organizations,
        }
    }
}
