/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// RouterStatus : How warm this user's routing memory is, and what depends on it.  Routing memory has no single warmth: it is a set of independent pools. ``default_pool`` is what a request with no ``Otari-Router-Task`` header votes over (every record the user has in this workspace, labeled or not) and ``tasks`` lists each partition, which only requests carrying that label use. Each crosses ``seed_count`` on its own.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct RouterStatus {
    #[serde(rename = "alpha")]
    pub alpha: f64,
    #[serde(rename = "confidence_floor")]
    pub confidence_floor: f64,
    #[serde(rename = "default_pool")]
    pub default_pool: Box<models::PoolStatus>,
    #[serde(rename = "embedding_model")]
    pub embedding_model: String,
    #[serde(rename = "granularity")]
    pub granularity: String,
    #[serde(rename = "k")]
    pub k: i32,
    #[serde(rename = "policies")]
    pub policies: Vec<models::LearnedPolicy>,
    #[serde(rename = "seed_count")]
    pub seed_count: i32,
    #[serde(rename = "tasks")]
    pub tasks: Vec<models::TaskPool>,
    #[serde(rename = "user_id")]
    pub user_id: String,
    #[serde(rename = "workspace_id")]
    pub workspace_id: uuid::Uuid,
}

impl RouterStatus {
    /// How warm this user's routing memory is, and what depends on it.  Routing memory has no single warmth: it is a set of independent pools. ``default_pool`` is what a request with no ``Otari-Router-Task`` header votes over (every record the user has in this workspace, labeled or not) and ``tasks`` lists each partition, which only requests carrying that label use. Each crosses ``seed_count`` on its own.
    pub fn new(
        alpha: f64,
        confidence_floor: f64,
        default_pool: models::PoolStatus,
        embedding_model: String,
        granularity: String,
        k: i32,
        policies: Vec<models::LearnedPolicy>,
        seed_count: i32,
        tasks: Vec<models::TaskPool>,
        user_id: String,
        workspace_id: uuid::Uuid,
    ) -> RouterStatus {
        RouterStatus {
            alpha,
            confidence_floor,
            default_pool: Box::new(default_pool),
            embedding_model,
            granularity,
            k,
            policies,
            seed_count,
            tasks,
            user_id,
            workspace_id,
        }
    }
}
