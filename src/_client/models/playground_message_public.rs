/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.12.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PlaygroundMessagePublic : One stored turn, in the order it was saved.  No usage figures, matching what the save accepts: tokens, cost and timing describe the request that ran rather than the conversation, and a resumed transcript reporting an old request's latency as this session's would be lying. The billing record for that request is its ``usage_logs`` row.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PlaygroundMessagePublic {
    #[serde(rename = "content")]
    pub content: String,
    #[serde(
        rename = "reasoning",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub reasoning: Option<Option<String>>,
    #[serde(rename = "role")]
    pub role: String,
}

impl PlaygroundMessagePublic {
    /// One stored turn, in the order it was saved.  No usage figures, matching what the save accepts: tokens, cost and timing describe the request that ran rather than the conversation, and a resumed transcript reporting an old request's latency as this session's would be lying. The billing record for that request is its ``usage_logs`` row.
    pub fn new(content: String, role: String) -> PlaygroundMessagePublic {
        PlaygroundMessagePublic {
            content,
            reasoning: None,
            role,
        }
    }
}
