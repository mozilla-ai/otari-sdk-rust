/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// OfferingUsage : What the viewer's organization actually paid for one offering, last 30 days.  The listed rate is what a token costs; this is what the tokens cost, which is lower wherever prompt caching hit. Absent for a visitor and for an offering the organization never called.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct OfferingUsage {
    /// Cache-read tokens over prompt tokens. Null when no prompt tokens.
    #[serde(rename = "cache_hit_rate", deserialize_with = "Option::deserialize")]
    pub cache_hit_rate: Option<f64>,
    #[serde(rename = "cache_read_tokens")]
    pub cache_read_tokens: i32,
    /// Spend over every token served, per million. Null when no tokens were served.
    #[serde(
        rename = "effective_price_per_million",
        deserialize_with = "Option::deserialize"
    )]
    pub effective_price_per_million: Option<f64>,
    #[serde(rename = "requests")]
    pub requests: i32,
    #[serde(rename = "spend_usd")]
    pub spend_usd: f64,
    #[serde(rename = "total_tokens")]
    pub total_tokens: i32,
}

impl OfferingUsage {
    /// What the viewer's organization actually paid for one offering, last 30 days.  The listed rate is what a token costs; this is what the tokens cost, which is lower wherever prompt caching hit. Absent for a visitor and for an offering the organization never called.
    pub fn new(
        cache_hit_rate: Option<f64>,
        cache_read_tokens: i32,
        effective_price_per_million: Option<f64>,
        requests: i32,
        spend_usd: f64,
        total_tokens: i32,
    ) -> OfferingUsage {
        OfferingUsage {
            cache_hit_rate,
            cache_read_tokens,
            effective_price_per_million,
            requests,
            spend_usd,
            total_tokens,
        }
    }
}
