/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// GatewaySettings : Operator-facing runtime settings surfaced to the dashboard.  The top-level flags are kept for the callers that read them directly (pricing warnings, the models/providers pages); ``config`` is the full read-only view with the settable/startup-only marking.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct GatewaySettings {
    #[serde(rename = "config")]
    pub config: Vec<models::ConfigField>,
    #[serde(rename = "default_pricing")]
    pub default_pricing: bool,
    /// Whether the dashboard master key is configured at startup or generated and stored by Otari.
    #[serde(rename = "master_key_source")]
    pub master_key_source: MasterKeySource,
    #[serde(rename = "mode")]
    pub mode: String,
    #[serde(rename = "model_discovery")]
    pub model_discovery: bool,
    #[serde(rename = "require_pricing")]
    pub require_pricing: bool,
    /// Whether OTARI_SECRET_KEY is set on the server. Provider credentials are encrypted at rest with it, so a deployment without it can store none. The dashboard reads the same fact from the membership context's provider_key_encryption_available, because this endpoint is operator-only and the provider-key pages are read by tenants.
    #[serde(rename = "secret_key_configured")]
    pub secret_key_configured: bool,
    #[serde(rename = "version")]
    pub version: String,
}

impl GatewaySettings {
    /// Operator-facing runtime settings surfaced to the dashboard.  The top-level flags are kept for the callers that read them directly (pricing warnings, the models/providers pages); ``config`` is the full read-only view with the settable/startup-only marking.
    pub fn new(
        config: Vec<models::ConfigField>,
        default_pricing: bool,
        master_key_source: MasterKeySource,
        mode: String,
        model_discovery: bool,
        require_pricing: bool,
        secret_key_configured: bool,
        version: String,
    ) -> GatewaySettings {
        GatewaySettings {
            config,
            default_pricing,
            master_key_source,
            mode,
            model_discovery,
            require_pricing,
            secret_key_configured,
            version,
        }
    }
}
/// Whether the dashboard master key is configured at startup or generated and stored by Otari.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum MasterKeySource {
    #[serde(rename = "configured")]
    Configured,
    #[serde(rename = "generated")]
    Generated,
}

impl Default for MasterKeySource {
    fn default() -> MasterKeySource {
        Self::Configured
    }
}
