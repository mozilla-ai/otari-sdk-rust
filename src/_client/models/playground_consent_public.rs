/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.6.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PlaygroundConsentPublic : What this identity has agreed the Playground may store.  Two flags rather than one, matching what the page asks for at the moment it asks: saving a transcript and recording a model preference are different disclosures (the second stores *both* models' full answers), and the old page asked about each separately at the point of use. An identity with no stored row reads back as both false.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PlaygroundConsentPublic {
    #[serde(rename = "store_comparisons", skip_serializing_if = "Option::is_none")]
    pub store_comparisons: Option<bool>,
    #[serde(
        rename = "store_conversations",
        skip_serializing_if = "Option::is_none"
    )]
    pub store_conversations: Option<bool>,
}

impl PlaygroundConsentPublic {
    /// What this identity has agreed the Playground may store.  Two flags rather than one, matching what the page asks for at the moment it asks: saving a transcript and recording a model preference are different disclosures (the second stores *both* models' full answers), and the old page asked about each separately at the point of use. An identity with no stored row reads back as both false.
    pub fn new() -> PlaygroundConsentPublic {
        PlaygroundConsentPublic {
            store_comparisons: None,
            store_conversations: None,
        }
    }
}
