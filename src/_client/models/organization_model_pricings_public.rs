/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// OrganizationModelPricingsPublic : One page of the organization's overrides, and how many there are in total.  The envelope shape the platform's equivalent endpoint returns, kept so the generated dashboard client stays recognizable across both trees. ``count`` is the total rather than the length of ``data``, which is what lets a client tell whether another page is owed.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct OrganizationModelPricingsPublic {
    #[serde(rename = "count")]
    pub count: i32,
    #[serde(rename = "data")]
    pub data: Vec<models::OrganizationModelPricingPublic>,
}

impl OrganizationModelPricingsPublic {
    /// One page of the organization's overrides, and how many there are in total.  The envelope shape the platform's equivalent endpoint returns, kept so the generated dashboard client stays recognizable across both trees. ``count`` is the total rather than the length of ``data``, which is what lets a client tell whether another page is owed.
    pub fn new(
        count: i32,
        data: Vec<models::OrganizationModelPricingPublic>,
    ) -> OrganizationModelPricingsPublic {
        OrganizationModelPricingsPublic { count, data }
    }
}
