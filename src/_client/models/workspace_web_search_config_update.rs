/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// WorkspaceWebSearchConfigUpdate : The configuration to store for a workspace, as a whole.  ``PUT`` semantics, ported from the hosted ``WorkspaceWebSearchConfigUpdate``: what is sent is what the workspace has afterwards, so an omitted field is cleared rather than left as it was.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct WorkspaceWebSearchConfigUpdate {
    /// Results are kept only from these domains; intersected with any list the request sends
    #[serde(
        rename = "allowed_domains",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub allowed_domains: Option<Option<Vec<String>>>,
    /// Results from these domains are dropped; added to any list the request sends
    #[serde(
        rename = "blocked_domains",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub blocked_domains: Option<Option<Vec<String>>>,
    /// False refuses web search for this workspace, both the otari_web_search tool and POST /v1/search. The fields below narrow the tool only.
    #[serde(rename = "enabled")]
    pub enabled: bool,
    /// Ceiling on results one search returns; only ever lowers the effective limit, so at most 20
    #[serde(
        rename = "max_results",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub max_results: Option<Option<i32>>,
    /// Provider-native request fields used as defaults (e.g. exa's 'type', searxng's 'engines').
    #[serde(
        rename = "provider_options",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub provider_options: Option<Option<std::collections::HashMap<String, serde_json::Value>>>,
    /// Hint used when a request declares otari_web_search without one of its own
    #[serde(
        rename = "purpose_hint",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub purpose_hint: Option<Option<String>>,
}

impl WorkspaceWebSearchConfigUpdate {
    /// The configuration to store for a workspace, as a whole.  ``PUT`` semantics, ported from the hosted ``WorkspaceWebSearchConfigUpdate``: what is sent is what the workspace has afterwards, so an omitted field is cleared rather than left as it was.
    pub fn new(enabled: bool) -> WorkspaceWebSearchConfigUpdate {
        WorkspaceWebSearchConfigUpdate {
            allowed_domains: None,
            blocked_domains: None,
            enabled,
            max_results: None,
            provider_options: None,
            purpose_hint: None,
        }
    }
}
