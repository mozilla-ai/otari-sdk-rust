/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.6.4
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// BuiltInGuardrailSpec : One guardrail this gateway can construct and run itself.  Upstream's own metadata model, extended rather than copied, so a field it adds is carried instead of waiting on an edit here. The four taxonomy enums document themselves in the published schema, which is why almost nothing below restates what a field name and its type already say; the descriptions that remain are on the answers only this gateway can give.  Inheriting also takes upstream's field serializers, which sort every set-valued field on the way out, so the JSON is stable across calls without sorting anything here.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct BuiltInGuardrailSpec {
    #[serde(rename = "alternate_backends", skip_serializing_if = "Option::is_none")]
    pub alternate_backends: Option<Vec<String>>,
    #[serde(rename = "backend")]
    pub backend: models::BackendType,
    #[serde(rename = "categories")]
    pub categories: Vec<String>,
    /// Constructor arguments, which is where a vendor API key and an endpoint live
    #[serde(rename = "create_parameters", skip_serializing_if = "Option::is_none")]
    pub create_parameters: Option<Vec<models::GuardrailParameterSpec>>,
    #[serde(rename = "default_license")]
    pub default_license: String,
    #[serde(rename = "description")]
    pub description: String,
    #[serde(rename = "display_name")]
    pub display_name: String,
    /// The any-guardrail class, and the name a stored guardrail selects
    #[serde(rename = "guardrail_name")]
    pub guardrail_name: String,
    #[serde(rename = "multilingual", skip_serializing_if = "Option::is_none")]
    pub multilingual: Option<bool>,
    #[serde(rename = "multimodal", skip_serializing_if = "Option::is_none")]
    pub multimodal: Option<bool>,
    #[serde(
        rename = "optional_validate_kwargs",
        skip_serializing_if = "Option::is_none"
    )]
    pub optional_validate_kwargs: Option<Vec<String>>,
    #[serde(rename = "output_shapes")]
    pub output_shapes: Vec<String>,
    #[serde(rename = "primary_category")]
    pub primary_category: models::GuardrailCategory,
    #[serde(
        rename = "required_validate_kwargs",
        skip_serializing_if = "Option::is_none"
    )]
    pub required_validate_kwargs: Option<Vec<String>>,
    /// One-of constraints that no single parameter's required flag can express. At least one member of each group must be supplied, or one of the environment variables that satisfies it
    #[serde(rename = "requirement_groups", skip_serializing_if = "Option::is_none")]
    pub requirement_groups: Option<Vec<models::RequirementGroup>>,
    #[serde(rename = "requires_api_key", skip_serializing_if = "Option::is_none")]
    pub requires_api_key: Option<bool>,
    #[serde(rename = "stages")]
    pub stages: Vec<String>,
    /// Whether several inputs run as one real batched call, not a per-item loop
    #[serde(rename = "supports_batch", skip_serializing_if = "Option::is_none")]
    pub supports_batch: Option<bool>,
    /// Per-call arguments, sent with the text on every check
    #[serde(
        rename = "validate_parameters",
        skip_serializing_if = "Option::is_none"
    )]
    pub validate_parameters: Option<Vec<models::GuardrailParameterSpec>>,
    #[serde(rename = "variant_licenses", skip_serializing_if = "Option::is_none")]
    pub variant_licenses: Option<Vec<std::collections::HashMap<String, String>>>,
    #[serde(rename = "vendor")]
    pub vendor: String,
}

impl BuiltInGuardrailSpec {
    /// One guardrail this gateway can construct and run itself.  Upstream's own metadata model, extended rather than copied, so a field it adds is carried instead of waiting on an edit here. The four taxonomy enums document themselves in the published schema, which is why almost nothing below restates what a field name and its type already say; the descriptions that remain are on the answers only this gateway can give.  Inheriting also takes upstream's field serializers, which sort every set-valued field on the way out, so the JSON is stable across calls without sorting anything here.
    pub fn new(
        backend: models::BackendType,
        categories: Vec<String>,
        default_license: String,
        description: String,
        display_name: String,
        guardrail_name: String,
        output_shapes: Vec<String>,
        primary_category: models::GuardrailCategory,
        stages: Vec<String>,
        vendor: String,
    ) -> BuiltInGuardrailSpec {
        BuiltInGuardrailSpec {
            alternate_backends: None,
            backend,
            categories,
            create_parameters: None,
            default_license,
            description,
            display_name,
            guardrail_name,
            multilingual: None,
            multimodal: None,
            optional_validate_kwargs: None,
            output_shapes,
            primary_category,
            required_validate_kwargs: None,
            requirement_groups: None,
            requires_api_key: None,
            stages,
            supports_batch: None,
            validate_parameters: None,
            variant_licenses: None,
            vendor,
        }
    }
}
