/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// AcceptInvitationResultPublic : What accepting produces: enough for the accept page to say where the visitor landed.  No session and no token: accepting resolves the membership to ``active`` and stops there. The identity it resolves to is password-less on the roster until it is claimed, so the next step is ``POST /v1/auth/signup`` on the invited address, not a sign-in.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct AcceptInvitationResultPublic {
    #[serde(rename = "organization_name")]
    pub organization_name: String,
    #[serde(rename = "role")]
    pub role: String,
}

impl AcceptInvitationResultPublic {
    /// What accepting produces: enough for the accept page to say where the visitor landed.  No session and no token: accepting resolves the membership to ``active`` and stops there. The identity it resolves to is password-less on the roster until it is claimed, so the next step is ``POST /v1/auth/signup`` on the invited address, not a sign-in.
    pub fn new(organization_name: String, role: String) -> AcceptInvitationResultPublic {
        AcceptInvitationResultPublic {
            organization_name,
            role,
        }
    }
}
