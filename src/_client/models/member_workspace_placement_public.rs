/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// MemberWorkspacePlacementPublic : One workspace a member is in, with their role and ceiling there.  A ceiling is keyed on the *membership*, not on the person, so a member of two workspaces has two of them. The membership id is carried in its own right rather than read back off the ceiling, because it is needed precisely when there is no ceiling yet and one is about to be created.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct MemberWorkspacePlacementPublic {
    #[serde(
        rename = "ceiling",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub ceiling: Option<Option<Box<models::MemberCeilingPublic>>>,
    #[serde(rename = "role")]
    pub role: String,
    #[serde(rename = "workspace_id")]
    pub workspace_id: uuid::Uuid,
    #[serde(rename = "workspace_member_id")]
    pub workspace_member_id: uuid::Uuid,
    #[serde(rename = "workspace_name")]
    pub workspace_name: String,
}

impl MemberWorkspacePlacementPublic {
    /// One workspace a member is in, with their role and ceiling there.  A ceiling is keyed on the *membership*, not on the person, so a member of two workspaces has two of them. The membership id is carried in its own right rather than read back off the ceiling, because it is needed precisely when there is no ceiling yet and one is about to be created.
    pub fn new(
        role: String,
        workspace_id: uuid::Uuid,
        workspace_member_id: uuid::Uuid,
        workspace_name: String,
    ) -> MemberWorkspacePlacementPublic {
        MemberWorkspacePlacementPublic {
            ceiling: None,
            role,
            workspace_id,
            workspace_member_id,
            workspace_name,
        }
    }
}
