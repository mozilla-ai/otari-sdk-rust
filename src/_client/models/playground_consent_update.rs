/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.6.4
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PlaygroundConsentUpdate : A partial update: an omitted flag is left as it was.  Tri-state on purpose. The page grants one flag at a time, just in time, so a request that carried both would silently re-assert the other, which is the wrong direction for a consent record to move on its own.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PlaygroundConsentUpdate {
    #[serde(
        rename = "store_comparisons",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub store_comparisons: Option<Option<bool>>,
    #[serde(
        rename = "store_conversations",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub store_conversations: Option<Option<bool>>,
}

impl PlaygroundConsentUpdate {
    /// A partial update: an omitted flag is left as it was.  Tri-state on purpose. The page grants one flag at a time, just in time, so a request that carried both would silently re-assert the other, which is the wrong direction for a consent record to move on its own.
    pub fn new() -> PlaygroundConsentUpdate {
        PlaygroundConsentUpdate {
            store_comparisons: None,
            store_conversations: None,
        }
    }
}
