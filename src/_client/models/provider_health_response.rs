/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ProviderHealthResponse : Provider connectivity across the whole gateway, for the health monitor.  Carries per-provider results plus the ``healthy`` / ``total`` counts and the most recent ``checked_at`` so the overview page can render a summary tile without re-deriving them.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ProviderHealthResponse {
    /// ISO 8601 time of the most recent per-provider check (null if none yet).
    #[serde(
        rename = "checked_at",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub checked_at: Option<Option<String>>,
    /// How many providers are not counted as reachable only because model discovery is unavailable for them. These may still serve requests.
    #[serde(rename = "degraded", skip_serializing_if = "Option::is_none")]
    pub degraded: Option<i32>,
    /// How many providers are currently reachable.
    #[serde(rename = "healthy")]
    pub healthy: i32,
    #[serde(rename = "providers")]
    pub providers: Vec<models::ProviderHealthSchema>,
    /// How many providers are configured.
    #[serde(rename = "total")]
    pub total: i32,
}

impl ProviderHealthResponse {
    /// Provider connectivity across the whole gateway, for the health monitor.  Carries per-provider results plus the ``healthy`` / ``total`` counts and the most recent ``checked_at`` so the overview page can render a summary tile without re-deriving them.
    pub fn new(
        healthy: i32,
        providers: Vec<models::ProviderHealthSchema>,
        total: i32,
    ) -> ProviderHealthResponse {
        ProviderHealthResponse {
            checked_at: None,
            degraded: None,
            healthy,
            providers,
            total,
        }
    }
}
