/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ExplainRequest : Ask what a policy would do, without dispatching anything.  Either name a stored/configured policy (``name``) or pass a draft ``spec`` that has not been saved. The draft form is what makes authoring-time validation possible: the compiler filters candidates, so a chain can compile down to one attempt, and an author needs to see that before saving rather than during an outage.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ExplainRequest {
    /// Simulate an API key's allow-list. Omit for unrestricted.
    #[serde(
        rename = "allowed_models",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub allowed_models: Option<Option<Vec<String>>>,
    /// Simulated budget remaining, USD.
    #[serde(
        rename = "budget_remaining_usd",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub budget_remaining_usd: Option<Option<f64>>,
    /// Simulated budget usage percentage.
    #[serde(
        rename = "budget_used_pct",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub budget_used_pct: Option<Option<f64>>,
    /// Evaluate conditions as this API key id.
    #[serde(
        rename = "key_id",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub key_id: Option<Option<String>>,
    /// An existing policy to explain.
    #[serde(
        rename = "name",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub name: Option<Option<String>>,
    /// Provider-native request fields used as defaults (e.g. exa's 'type', searxng's 'engines').
    #[serde(
        rename = "spec",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub spec: Option<Option<std::collections::HashMap<String, serde_json::Value>>>,
    /// Evaluate conditions as this user.
    #[serde(
        rename = "user_id",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub user_id: Option<Option<String>>,
    /// Resolve `name` and the policy's candidate selectors in this workspace. Omit for the deployment's default workspace.
    #[serde(
        rename = "workspace_id",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub workspace_id: Option<Option<uuid::Uuid>>,
}

impl ExplainRequest {
    /// Ask what a policy would do, without dispatching anything.  Either name a stored/configured policy (``name``) or pass a draft ``spec`` that has not been saved. The draft form is what makes authoring-time validation possible: the compiler filters candidates, so a chain can compile down to one attempt, and an author needs to see that before saving rather than during an outage.
    pub fn new() -> ExplainRequest {
        ExplainRequest {
            allowed_models: None,
            budget_remaining_usd: None,
            budget_used_pct: None,
            key_id: None,
            name: None,
            spec: None,
            user_id: None,
            workspace_id: None,
        }
    }
}
