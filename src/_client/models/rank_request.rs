/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// RankRequest : Record how well each candidate did, for one or many prompts.  A batch because of the arithmetic: a pool routes nothing until it holds ``router_seed_count`` examples (20 by default) and the vote reads the ``k`` nearest (5 by default), so a useful first teaching pass is dozens of examples across the kinds of prompt you care about.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct RankRequest {
    /// The scored prompts to record.
    #[serde(rename = "examples")]
    pub examples: Vec<models::ScoredExample>,
    /// Whose routing memory these examples belong to.
    #[serde(rename = "user_id")]
    pub user_id: String,
    /// Which workspace's routing memory these examples belong to. Omit for the deployment's default workspace. Only requests billing to that workspace vote over them.
    #[serde(
        rename = "workspace_id",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub workspace_id: Option<Option<uuid::Uuid>>,
}

impl RankRequest {
    /// Record how well each candidate did, for one or many prompts.  A batch because of the arithmetic: a pool routes nothing until it holds ``router_seed_count`` examples (20 by default) and the vote reads the ``k`` nearest (5 by default), so a useful first teaching pass is dozens of examples across the kinds of prompt you care about.
    pub fn new(examples: Vec<models::ScoredExample>, user_id: String) -> RankRequest {
        RankRequest {
            examples,
            user_id,
            workspace_id: None,
        }
    }
}
