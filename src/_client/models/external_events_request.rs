/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ExternalEventsRequest : A batch of imported usage events sharing a source and default user.  ``extra=\"forbid\"`` here mirrors the per-event schema: a stray content field at the batch level (e.g. a top-level ``prompt``) is a 422, not silently ignored.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ExternalEventsRequest {
    #[serde(rename = "events")]
    pub events: Vec<models::ExternalUsageEvent>,
    #[serde(rename = "source")]
    pub source: String,
    #[serde(
        rename = "user_id",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub user_id: Option<Option<String>>,
}

impl ExternalEventsRequest {
    /// A batch of imported usage events sharing a source and default user.  ``extra=\"forbid\"`` here mirrors the per-event schema: a stray content field at the batch level (e.g. a top-level ``prompt``) is a 422, not silently ignored.
    pub fn new(events: Vec<models::ExternalUsageEvent>, source: String) -> ExternalEventsRequest {
        ExternalEventsRequest {
            events,
            source,
            user_id: None,
        }
    }
}
