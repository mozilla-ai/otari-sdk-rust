/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// AgentTelemetryOutcomes : How much work the agent produced inside the window.  Each value is the read-time increment for its metric: a delta series is summed, a cumulative one is diffed per series generation, so a counter that is re-reported in full on every export is never counted twice. ``lines_of_code`` sums the added and removed series, which are stored separately. ``active_time`` is in seconds, as the agent reports it.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct AgentTelemetryOutcomes {
    #[serde(rename = "active_time", skip_serializing_if = "Option::is_none")]
    pub active_time: Option<f64>,
    #[serde(rename = "commits", skip_serializing_if = "Option::is_none")]
    pub commits: Option<f64>,
    #[serde(rename = "lines_of_code", skip_serializing_if = "Option::is_none")]
    pub lines_of_code: Option<f64>,
    #[serde(rename = "pull_requests", skip_serializing_if = "Option::is_none")]
    pub pull_requests: Option<f64>,
}

impl AgentTelemetryOutcomes {
    /// How much work the agent produced inside the window.  Each value is the read-time increment for its metric: a delta series is summed, a cumulative one is diffed per series generation, so a counter that is re-reported in full on every export is never counted twice. ``lines_of_code`` sums the added and removed series, which are stored separately. ``active_time`` is in seconds, as the agent reports it.
    pub fn new() -> AgentTelemetryOutcomes {
        AgentTelemetryOutcomes {
            active_time: None,
            commits: None,
            lines_of_code: None,
            pull_requests: None,
        }
    }
}
