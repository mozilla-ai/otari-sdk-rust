/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// UsageErrorCodeRow : One error-taxonomy row: the failures in the window sharing a status code.  ``status_code`` is None for failures recorded without one (rows written before the column existed, and failures no HTTP status describes, e.g. a stream that finished without usage data under the ``fail`` policy). ``error_class`` is the coarse display bucket derived from the code, so a UI can group \"provider fault\" against \"my own misconfiguration\" without re-deriving a status ladder; the raw code stays alongside it for precision.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct UsageErrorCodeRow {
    #[serde(rename = "error_class")]
    pub error_class: ErrorClass,
    #[serde(rename = "requests")]
    pub requests: i32,
    #[serde(rename = "status_code", deserialize_with = "Option::deserialize")]
    pub status_code: Option<i32>,
}

impl UsageErrorCodeRow {
    /// One error-taxonomy row: the failures in the window sharing a status code.  ``status_code`` is None for failures recorded without one (rows written before the column existed, and failures no HTTP status describes, e.g. a stream that finished without usage data under the ``fail`` policy). ``error_class`` is the coarse display bucket derived from the code, so a UI can group \"provider fault\" against \"my own misconfiguration\" without re-deriving a status ladder; the raw code stays alongside it for precision.
    pub fn new(
        error_class: ErrorClass,
        requests: i32,
        status_code: Option<i32>,
    ) -> UsageErrorCodeRow {
        UsageErrorCodeRow {
            error_class,
            requests,
            status_code,
        }
    }
}
///
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum ErrorClass {
    #[serde(rename = "pricing")]
    Pricing,
    #[serde(rename = "rate_limit")]
    RateLimit,
    #[serde(rename = "auth")]
    Auth,
    #[serde(rename = "provider_error")]
    ProviderError,
    #[serde(rename = "client_error")]
    ClientError,
    #[serde(rename = "unknown")]
    Unknown,
}

impl Default for ErrorClass {
    fn default() -> ErrorClass {
        Self::Pricing
    }
}
