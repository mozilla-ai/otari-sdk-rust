/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.12.1
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// CheckVerdictRequest : One verifier gate's verdict, as the caller's own verifier run produced it.  Mirrors ``JudgeVerdictRequest`` field-for-field: ``gate_id`` echoes back the gate the policy itself named (same bound, same reason), ``outcome`` is the caller's own report of the verifier's exit code (0/1/anything else, mapped by the caller to pass/fail/error), and ``detail`` is the verifier's captured stdout, capped the same way ``reasoning`` is.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CheckVerdictRequest {
    #[serde(rename = "detail", skip_serializing_if = "Option::is_none")]
    pub detail: Option<String>,
    #[serde(rename = "gate_id")]
    pub gate_id: String,
    #[serde(rename = "outcome")]
    pub outcome: Outcome,
}

impl CheckVerdictRequest {
    /// One verifier gate's verdict, as the caller's own verifier run produced it.  Mirrors ``JudgeVerdictRequest`` field-for-field: ``gate_id`` echoes back the gate the policy itself named (same bound, same reason), ``outcome`` is the caller's own report of the verifier's exit code (0/1/anything else, mapped by the caller to pass/fail/error), and ``detail`` is the verifier's captured stdout, capped the same way ``reasoning`` is.
    pub fn new(gate_id: String, outcome: Outcome) -> CheckVerdictRequest {
        CheckVerdictRequest {
            detail: None,
            gate_id,
            outcome,
        }
    }
}
///
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Outcome {
    #[serde(rename = "pass")]
    Pass,
    #[serde(rename = "fail")]
    Fail,
    #[serde(rename = "error")]
    Error,
}

impl Default for Outcome {
    fn default() -> Outcome {
        Self::Pass
    }
}
