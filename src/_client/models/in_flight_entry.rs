/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// InFlightEntry : One request the gateway is serving right now.  Field names match their ``UsageEntry`` counterparts so a request reads the same way in flight as it does once it has settled. ``id`` is the exception: it is an ephemeral tracking id, not the id of the usage row this will become.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct InFlightEntry {
    #[serde(rename = "api_key_id", deserialize_with = "Option::deserialize")]
    pub api_key_id: Option<String>,
    #[serde(rename = "elapsed_ms")]
    pub elapsed_ms: i32,
    #[serde(rename = "endpoint")]
    pub endpoint: String,
    #[serde(rename = "id")]
    pub id: String,
    #[serde(rename = "model")]
    pub model: String,
    #[serde(rename = "policy_name", deserialize_with = "Option::deserialize")]
    pub policy_name: Option<String>,
    #[serde(rename = "provider", deserialize_with = "Option::deserialize")]
    pub provider: Option<String>,
    #[serde(rename = "started_at")]
    pub started_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "user_id", deserialize_with = "Option::deserialize")]
    pub user_id: Option<String>,
}

impl InFlightEntry {
    /// One request the gateway is serving right now.  Field names match their ``UsageEntry`` counterparts so a request reads the same way in flight as it does once it has settled. ``id`` is the exception: it is an ephemeral tracking id, not the id of the usage row this will become.
    pub fn new(
        api_key_id: Option<String>,
        elapsed_ms: i32,
        endpoint: String,
        id: String,
        model: String,
        policy_name: Option<String>,
        provider: Option<String>,
        started_at: chrono::DateTime<chrono::FixedOffset>,
        user_id: Option<String>,
    ) -> InFlightEntry {
        InFlightEntry {
            api_key_id,
            elapsed_ms,
            endpoint,
            id,
            model,
            policy_name,
            provider,
            started_at,
            user_id,
        }
    }
}
