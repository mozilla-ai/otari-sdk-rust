/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// UsageGroupRow : One breakdown row (a model, a user, an API key, a session, ...).  ``key`` is None both for the synthesized fold row (``is_other=True``) and for a real group whose column was NULL (e.g. usage from a since-deleted user, with ``is_other=False``). ``is_other`` disambiguates the two so the UI does not mislabel deleted-user usage as the fold.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct UsageGroupRow {
    #[serde(rename = "cost")]
    pub cost: f64,
    #[serde(rename = "is_other", skip_serializing_if = "Option::is_none")]
    pub is_other: Option<bool>,
    #[serde(rename = "key", deserialize_with = "Option::deserialize")]
    pub key: Option<String>,
    #[serde(
        rename = "label",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub label: Option<Option<String>>,
    #[serde(rename = "requests")]
    pub requests: i32,
    #[serde(rename = "tokens")]
    pub tokens: i32,
}

impl UsageGroupRow {
    /// One breakdown row (a model, a user, an API key, a session, ...).  ``key`` is None both for the synthesized fold row (``is_other=True``) and for a real group whose column was NULL (e.g. usage from a since-deleted user, with ``is_other=False``). ``is_other`` disambiguates the two so the UI does not mislabel deleted-user usage as the fold.
    pub fn new(cost: f64, key: Option<String>, requests: i32, tokens: i32) -> UsageGroupRow {
        UsageGroupRow {
            cost,
            is_other: None,
            key,
            label: None,
            requests,
            tokens,
        }
    }
}
