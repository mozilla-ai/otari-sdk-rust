/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// SearchProviderSchema : One search provider this build can dispatch to, for the add-tool picker.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct SearchProviderSchema {
    /// The endpoint a tool on this provider uses when it declares no api_base. Null means nothing supplies one, so an api_base is required.
    #[serde(
        rename = "default_api_base",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub default_api_base: Option<Option<String>>,
    /// Value to send as 'provider'.
    #[serde(rename = "id")]
    pub id: String,
    /// True when this provider has no endpoint of its own, so the tool must say where the backend is.
    #[serde(rename = "requires_api_base")]
    pub requires_api_base: bool,
    /// True when a tool on this provider must carry an API key.
    #[serde(rename = "requires_api_key")]
    pub requires_api_key: bool,
}

impl SearchProviderSchema {
    /// One search provider this build can dispatch to, for the add-tool picker.
    pub fn new(
        id: String,
        requires_api_base: bool,
        requires_api_key: bool,
    ) -> SearchProviderSchema {
        SearchProviderSchema {
            default_api_base: None,
            id,
            requires_api_base,
            requires_api_key,
        }
    }
}
