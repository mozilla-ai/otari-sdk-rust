/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// LearnedPolicy : A policy whose selection depends on routing memory, for the status overview.  Only a *learned* policy belongs here. A weighted policy names a router too, but its split is written in the policy document, so listing it under a warmth report would tie it to a pool it never reads.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct LearnedPolicy {
    #[serde(rename = "backend")]
    pub backend: String,
    #[serde(rename = "candidates")]
    pub candidates: Vec<String>,
    #[serde(rename = "default_target")]
    pub default_target: String,
    #[serde(rename = "name")]
    pub name: String,
}

impl LearnedPolicy {
    /// A policy whose selection depends on routing memory, for the status overview.  Only a *learned* policy belongs here. A weighted policy names a router too, but its split is written in the policy document, so listing it under a warmth report would tie it to a pool it never reads.
    pub fn new(
        backend: String,
        candidates: Vec<String>,
        default_target: String,
        name: String,
    ) -> LearnedPolicy {
        LearnedPolicy {
            backend,
            candidates,
            default_target,
            name,
        }
    }
}
