/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.0.0-dev
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// SendTestMailResponse : The outcome of one test send.  ``reason`` carries the transport's own error text, which is the whole value of a test button (an operator needs to know it was a refused login rather than a wrong port). Safe here and only here: this endpoint is master-key gated, unlike the public error paths that must not leak internals.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct SendTestMailResponse {
    #[serde(rename = "ok")]
    pub ok: bool,
    #[serde(
        rename = "reason",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub reason: Option<Option<String>>,
    #[serde(rename = "transport")]
    pub transport: String,
}

impl SendTestMailResponse {
    /// The outcome of one test send.  ``reason`` carries the transport's own error text, which is the whole value of a test button (an operator needs to know it was a refused login rather than a wrong port). Safe here and only here: this endpoint is master-key gated, unlike the public error paths that must not leak internals.
    pub fn new(ok: bool, transport: String) -> SendTestMailResponse {
        SendTestMailResponse {
            ok,
            reason: None,
            transport,
        }
    }
}
