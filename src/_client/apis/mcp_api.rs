/*
 * otari
 *
 * Otari, an OpenAI-compatible LLM gateway with API key management
 *
 * The version of the OpenAPI document: 0.6.4
 *
 * Generated by: https://openapi-generator.tech
 */

use super::{configuration, ContentType, Error};
use crate::{apis::ResponseContent, models};
use reqwest;
use serde::{de::Error as _, Deserialize, Serialize};

/// struct for typed errors of method [`mcp_execute_mcp_tool`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum McpExecuteMcpToolError {
    Status400(models::McpErrorBody),
    Status401(models::McpErrorBody),
    Status402(models::McpErrorBody),
    Status403(models::McpErrorBody),
    Status404(models::McpErrorBody),
    Status409(models::McpErrorBody),
    Status422(models::McpErrorBody),
    Status429(models::McpErrorBody),
    Status500(models::McpErrorBody),
    Status502(models::McpErrorBody),
    Status503(models::McpErrorBody),
    Status504(models::McpErrorBody),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`mcp_list_mcp_tools`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum McpListMcpToolsError {
    Status400(models::McpErrorBody),
    Status401(models::McpErrorBody),
    Status402(models::McpErrorBody),
    Status403(models::McpErrorBody),
    Status404(models::McpErrorBody),
    Status422(models::McpErrorBody),
    Status429(models::McpErrorBody),
    Status500(models::McpErrorBody),
    Status502(models::McpErrorBody),
    Status503(models::McpErrorBody),
    UnknownValue(serde_json::Value),
}

/// Execute one caller-authorized tool call against a stored MCP server.  The calling application owns any user approval, argument editing, cancellation and action history; Otari executes exactly the tool name and arguments it is given, once, and returns the remote server's native result. A result with ``isError: true`` is a definitive outcome and comes back as an HTTP 200.  **This request must never be retried automatically.** ``client_execution_id`` is correlation, not idempotency: once the call has been dispatched Otari cannot know whether the tool ran, and an ``outcome_unknown`` response means exactly that. Proxies, service meshes and SDKs on this path have to disable retries for it, including on connection resets and 5xx responses.
pub async fn mcp_execute_mcp_tool(
    configuration: &configuration::Configuration,
    mcp_execute_request: models::McpExecuteRequest,
) -> Result<models::CallToolResult, Error<McpExecuteMcpToolError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_body_mcp_execute_request = mcp_execute_request;

    let uri_str = format!("{}/api/v1/mcp/execute", configuration.base_path);
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref apikey) = configuration.api_key {
        let key = apikey.key.clone();
        let value = match apikey.prefix {
            Some(ref prefix) => format!("{} {}", prefix, key),
            None => key,
        };
        req_builder = req_builder.header("x-api-key", value);
    };
    if let Some(ref apikey) = configuration.api_key {
        let key = apikey.key.clone();
        let value = match apikey.prefix {
            Some(ref prefix) => format!("{} {}", prefix, key),
            None => key,
        };
        req_builder = req_builder.header("Otari-Key", value);
    };
    req_builder = req_builder.json(&p_body_mcp_execute_request);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::CallToolResult`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::CallToolResult`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<McpExecuteMcpToolError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}

/// List the tools a stored MCP server exposes to the authenticated workspace.  Call this once per server when preparing a model or workflow run, and reuse the answer for every tool from that server for the length of the run: there is no cross-run cache in this version, so a later run rediscovers and staleness stays bounded without any invalidation state to keep.  The response is the whole authorized catalog or an error. It is never partial, because a caller would read a short catalog as the complete input to its own authorization and risk policy. The single exception is ``warnings``, which names a tool whose descriptor Otari could not carry.  A tool the server removes after discovery may still be proposed from the run's snapshot; execution then returns the remote server's own typed error.
pub async fn mcp_list_mcp_tools(
    configuration: &configuration::Configuration,
    mcp_server_id: &str,
) -> Result<models::McpToolsResponse, Error<McpListMcpToolsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_mcp_server_id = mcp_server_id;

    let uri_str = format!(
        "{}/api/v1/mcp/servers/{mcp_server_id}/tools",
        configuration.base_path,
        mcp_server_id = crate::apis::urlencode(p_path_mcp_server_id)
    );
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref apikey) = configuration.api_key {
        let key = apikey.key.clone();
        let value = match apikey.prefix {
            Some(ref prefix) => format!("{} {}", prefix, key),
            None => key,
        };
        req_builder = req_builder.header("x-api-key", value);
    };
    if let Some(ref apikey) = configuration.api_key {
        let key = apikey.key.clone();
        let value = match apikey.prefix {
            Some(ref prefix) => format!("{} {}", prefix, key),
            None => key,
        };
        req_builder = req_builder.header("Otari-Key", value);
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::McpToolsResponse`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::McpToolsResponse`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<McpListMcpToolsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}
